var layDateObject = {/*日期插件共用js*/    layDate : function (id, option) {/*日历控件*/        var config = $.extend({            elem: '#'+id,            event: 'focus',            format: 'YYYY-MM-DD hh:mm:ss',            // min: minDate, //设定最小日期            // max: maxDate, //设定最大日期            istime: true,            istoday: false,            choose: function (datas) {            }        }, option);        laydate(config);        return config;    }};